; Public overrides para uploads (quando o PHP estiver rodando como FPM/CGI que suporte .user.ini)
upload_max_filesize = 50M
post_max_size = 55M
;Opcional: garantir memória suficiente para processar o upload
memory_limit = 256M
